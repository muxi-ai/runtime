schema: "1.0.0"
id: test-docker-compose
description: "Test formation for Docker Compose"

llm:
  api_keys:
    openai: "sk-test-dummy-key"
  models:
    - text: "openai/gpt-4o-mini"

agents:
  - id: assistant
    name: "Docker Test Assistant"
    description: "Test agent for Docker Compose validation"
    system_message: "You are a helpful AI assistant for testing."
