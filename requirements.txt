# Core dependencies
pyyaml>=6.0
pydantic>=2.0.0
httpx>=0.24.0
anyio>=3.7.0
python-dotenv>=1.0.0
websockets>=11.0.3
httpx-sse>=0.4.0
rich>=13.6.0
colorama>=0.4.6
platformdirs>=4.0.0
psutil>=5.9.0  # System and process utilities

# LLM Package
onellm>=0.1.0

# A2A (Agent-to-Agent) Communication
a2a-sdk>=0.2  # Google's Agent-to-Agent SDK for inter-agent communication

# MCP Package
mcp>=1.9.0

# File processing and utilities
aiofiles>=23.2.0
python-magic>=0.4.27


# Document processing
markitdown[all]>=0.1.0  # Convert various file formats (PDF, Word, Excel, PowerPoint, etc.) to Markdown
pypdf2>=3.0.0  # PDF document processing
python-docx>=1.1.0  # Word document processing
markdownify>=0.11.6  # HTML to Markdown conversion
nltk>=3.8.0  # Natural language processing for chunking
spacy>=3.8.0  # Advanced NLP for semantic chunking (compatible with NumPy 2.x)
# TODO: After installing, run: python -m spacy download en_core_web_sm
sentence-transformers>=2.2.0  # Embedding generation for semantic search

# Image processing
Pillow>=10.0.0  # PIL/Pillow for image processing
pdf2image>=1.16.0  # Convert PDF pages to images

# Development tools
pytest>=7.0.0

# Server components
fastapi>=0.108.0
uvicorn>=0.24.0
starlette>=0.27.0

# Vector search
faiss-cpu>=1.10.0
faissx>=0.0.3  # Distributed FAISS for remote vector operations
pgvector>=0.3.6
numpy>=1.24.0
sqlite-vec>=0.1.6  # SQLite vector extension for similarity search

# Database
psycopg2-binary>=2.9.9
SQLAlchemy[asyncio]>=2.0.17

# Legacy dependencies (keeping for compatibility)
aiohttp>=3.8.0
asyncio>=3.4.3
click>=8.1.0
openai>=1.3.0

# For development with all dependencies:
# pip install -e packages/core[all]
