# Core dependencies
pyyaml>=6.0
pydantic>=2.0.0
httpx>=0.24.0
anyio>=3.7.0
python-dotenv>=1.0.0
websockets>=11.0.3
httpx-sse>=0.4.0
rich>=13.6.0
colorama>=0.4.6
platformdirs>=4.0.0

# MCP Package
mcp>=1.4.1  # Now available on PyPI

# File processing and utilities
aiofiles>=23.2.0
python-magic>=0.4.27
kafka>=1.3.0

# LLM Package
onellm>=0.0.1

# A2A (Agent-to-Agent) Communication
a2a-sdk>=0.0.1  # Google's Agent-to-Agent SDK for inter-agent communication

# Document processing
markitdown[all]>=0.1.0  # Convert various file formats (PDF, Word, Excel, PowerPoint, etc.) to Markdown
pypdf2>=3.0.0  # PDF document processing
python-docx>=1.1.0  # Word document processing
markdownify>=0.11.6  # HTML to Markdown conversion
nltk>=3.8.0  # Natural language processing for chunking
spacy>=3.8.0  # Advanced NLP for semantic chunking (compatible with NumPy 2.x)
sentence-transformers>=2.2.0  # Embedding generation for semantic search

# Development tools
pytest>=7.0.0

# Server components
fastapi>=0.104.0
uvicorn>=0.23.1
starlette>=0.27.0

# Vector search
faiss-cpu>=1.10.0
faissx>=0.0.1  # Distributed FAISS for remote vector operations
pgvector>=0.3.6
numpy>=1.24.0
sqlite-vec>=0.1.6  # SQLite vector extension for similarity search

# Database
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.17

# Observability and transport dependencies
msgpack>=1.0.0  # Binary serialization for observability events
protobuf>=4.0.0  # Protocol Buffers for structured data serialization
aiohttp>=3.8.0  # Async HTTP client for stream transport and health checks
kafka-python>=2.0.0  # Kafka client for stream transport
pyzmq>=25.0.0  # ZeroMQ for high-performance messaging
requests>=2.28.0  # HTTP client for health monitoring

# Utility libraries
nanoid>=2.0.0  # Unique ID generation for memory service

# For development with all dependencies:
# pip install -e packages/core[all]
