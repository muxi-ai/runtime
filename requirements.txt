# Core dependencies - install via: pip install muxi-runtime
# Or for development: pip install -e ".[dev]"
#
# This file is kept for compatibility with tools that expect requirements.txt
# The canonical dependency list is in pyproject.toml

mcp>=1.23.0
starlette>=0.49.1
aiohttp>=3.13.3
bokeh>=3.8.2
