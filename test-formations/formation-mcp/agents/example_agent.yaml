schema: "1.0.0"
id: "example_agent"
name: "Example Agent"
description: "AI assistant designed to test mcp servers"

system_message: |
  You are an AI assistant designed to test mcp servers.
  You should be able to use the mcp servers to answer questions and help the user.

role: "general"
specialties:
  - "mcp"
  - "mcp_servers"

# Enable MCP access for this agent
mcp_enabled: true
