
schema: "1.0.0"
id: "filesystem-mcp"
description: "Local filesystem tools"
active: true

type: "command"
install: "npm install -g @modelcontextprotocol/server-filesystem"

command: "npx"
args: ["-y", "@modelcontextprotocol/server-filesystem", "/Users/ran/Desktop/tests"]
timeout_seconds: 60
