schema: "1.0.0"
id: "web_tools"
description: "External web tools MCP server for web search and API requests"
active: true
type: "http"

endpoint: "http://localhost:8080"
timeout_seconds: 30
retry_attempts: 3

auth:
  type: "bearer"
  token: "${{ secrets.WEB_TOOLS_TOKEN }}"