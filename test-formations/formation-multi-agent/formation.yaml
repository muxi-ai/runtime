schema: "1.0.0"
id: "multi-agent-test"
description: "Multiple agents for testing routing and collaboration"

runtime:
  built_in_mcps: false  # Disable built-in MCPs for faster test startup

llm:
  settings:
    temperature: 0.7
    max_tokens: 3000
  api_keys:
    openai: "${{ secrets.OPENAI_API_KEY }}"
    anthropic: "${{ secrets.ANTHROPIC_API_KEY }}"
  models:
    - text: "openai/gpt-4o"

overlord:
  llm:
    model: "openai/gpt-4o-mini"
    settings:
      temperature: 0.2
      max_tokens: 2000
  config:
    max_tool_calls: -1

memory:
  buffer:
    size: 100
    multiplier: 10
    vector_search: true

a2a:
  enabled: false

logging:
  streams:
    - transport: "stdout"
      level: "debug"
      format: "jsonl"

# Agents and A2A configurations will be auto-discovered from directories