# Build SIF artifacts from existing GHCR Docker images and upload to S3.
#
# Manual trigger only. Converts Docker images already pushed to GHCR
# into SIF files and uploads them to s3://<bucket>/runtime/releases/<version>/.
#
# This workflow does NOT build Docker images or bump versions — it
# assumes the images already exist in GHCR from a prior release run.
#
# Use case: testing SIF→S3 pipeline, or re-uploading SIFs for a
# version whose Docker images are already published.
#
name: SIF to S3

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Runtime version (e.g. 0.20260423.5) — Docker images must already exist on GHCR'
        required: true
        type: string
      variants:
        description: 'Which variants to build'
        required: true
        type: choice
        options:
          - all
          - base
          - pytorch
          - cuda
        default: all

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: muxi-ai/runtime

jobs:
  # ===== Base SIF (amd64) =====
  sif-base-amd64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'base' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost /usr/local/lib/android
          sudo rm -rf /usr/share/swift /usr/local/julia* /opt/hostedtoolcache
          sudo apt-get clean
          df -h

      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-linux-amd64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-amd64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-linux-amd64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-linux-amd64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== Base SIF (arm64) =====
  sif-base-arm64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'base' }}
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: read
    steps:
      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-linux-arm64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-arm64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-linux-arm64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-linux-arm64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== PyTorch SIF (amd64) =====
  sif-pytorch-amd64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'pytorch' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost /usr/local/lib/android
          sudo rm -rf /usr/share/swift /usr/local/julia* /opt/hostedtoolcache
          sudo apt-get clean
          df -h

      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-pytorch-linux-amd64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-pytorch-amd64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-pytorch-linux-amd64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-pytorch-linux-amd64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== PyTorch SIF (arm64) =====
  sif-pytorch-arm64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'pytorch' }}
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: read
    steps:
      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-pytorch-linux-arm64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-pytorch-arm64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-pytorch-linux-arm64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-pytorch-linux-arm64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== CUDA SIF (amd64, experimental) =====
  sif-cuda-amd64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'cuda' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost /usr/local/lib/android
          sudo rm -rf /usr/share/swift /usr/local/julia* /opt/hostedtoolcache
          sudo apt-get clean
          df -h

      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-cuda-linux-amd64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-cuda-amd64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-cuda-linux-amd64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-cuda-linux-amd64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== CUDA SIF (arm64, experimental) =====
  sif-cuda-arm64:
    if: ${{ inputs.variants == 'all' || inputs.variants == 'cuda' }}
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: read
    steps:
      - name: Install Apptainer
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:apptainer/ppa
          sudo apt-get update
          sudo apt-get install -y apptainer

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build SIF
        run: |
          apptainer build muxi-runtime-${{ inputs.version }}-cuda-linux-arm64.sif \
            docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ inputs.version }}-cuda-arm64
          ls -lh *.sif

      - name: Upload to S3
        run: |
          pip install awscli
          aws s3 cp muxi-runtime-${{ inputs.version }}-cuda-linux-arm64.sif \
            s3://${{ secrets.MUXI_S3_BUCKET }}/runtime/releases/${{ inputs.version }}/muxi-runtime-${{ inputs.version }}-cuda-linux-arm64.sif
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.MUXI_S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.MUXI_S3_SECRET_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.MUXI_S3_ENDPOINT }}

  # ===== Summary =====
  summary:
    runs-on: ubuntu-latest
    if: always()
    needs: [sif-base-amd64, sif-base-arm64, sif-pytorch-amd64, sif-pytorch-arm64, sif-cuda-amd64, sif-cuda-arm64]
    steps:
      - name: Report
        run: |
          echo "SIF to S3 — version ${{ inputs.version }}, variants: ${{ inputs.variants }}"
          echo ""
          echo "S3 path: s3://<bucket>/runtime/releases/${{ inputs.version }}/"
          echo ""
          echo "Job results:"
          echo "  base-amd64:    ${{ needs.sif-base-amd64.result }}"
          echo "  base-arm64:    ${{ needs.sif-base-arm64.result }}"
          echo "  pytorch-amd64: ${{ needs.sif-pytorch-amd64.result }}"
          echo "  pytorch-arm64: ${{ needs.sif-pytorch-arm64.result }}"
          echo "  cuda-amd64:    ${{ needs.sif-cuda-amd64.result }}"
          echo "  cuda-arm64:    ${{ needs.sif-cuda-arm64.result }}"
