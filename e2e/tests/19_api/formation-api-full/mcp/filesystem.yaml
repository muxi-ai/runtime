schema: "1.0.0"
id: "filesystem-test"
description: "Local filesystem tools for API testing"

type: "command"

command: "npx"
args: ["-y", "@modelcontextprotocol/server-filesystem", "/tmp/muxi-test"]
timeout_seconds: 60
