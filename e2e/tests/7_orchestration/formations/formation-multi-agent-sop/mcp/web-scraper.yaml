schema: "1.0.0"
id: "web-scraper-mcp"
description: "Web scraper using firecrawl"

type: "command"

command: "npx"
args: ["-y", "firecrawl-mcp"]
timeout_seconds: 60

# Authentication (optional)
auth:
  type: "env"
  FIRECRAWL_API_KEY: "${{ secrets.FIRECRAWL_API_KEY }}"
