---
schema: "1.0.0"
id: "test-cache-enabled"
description: "Test formation with caching enabled (default settings)"

llm:
  settings:
    temperature: 0.7
    max_tokens: 100
    # Caching enabled with defaults (should use production settings)

  api_keys:
    openai: "${{ secrets.OPENAI_API_KEY }}"

  models:
    - text: "openai/gpt-4o-mini"
      settings:
        temperature: 0.7

agents:
  - schema: "1.0.0"
    id: "test-agent"
    name: "Test Agent"
    description: "Simple test agent for caching tests"
    system_message: "You are a test assistant. Keep responses brief."

memory:
  buffer:
    size: 5
    vector_search: false
