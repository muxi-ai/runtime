# Invalid agent schema for testing validation errors
schema: "2.0.0"  # Invalid version
id: "invalid-agent"
# Missing required 'name' field
# Missing required 'description' field
system_message: "This agent schema is intentionally invalid for testing"

# Invalid fields
invalid_field: "this should not be here"
llm_models:
  - invalid_model_type: "openai/gpt-4o"  # Should be 'text', 'vision', etc.
