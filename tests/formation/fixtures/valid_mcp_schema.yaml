# Valid MCP server schema for testing
schema: "1.0.0"
id: "fixture-test-mcp"
description: "A test MCP server loaded from a YAML fixture file"
type: "command"

# Command configuration
command: "python"
args: ["-m", "test_mcp_server"]
working_directory: "/tmp/test"

# Environment variables
env:
  DEBUG: "true"
  TEST_MODE: "enabled"

# Timeout configuration
timeout_seconds: 45
retry_attempts: 3
