schema: "99.99.99"  # Invalid schema version
id: "invalid-schema-test"
description: "This formation has an invalid schema version"

agents:
  - id: "assistant"
    model: "openai/gpt-5-nano"

memory:
  buffer:
    size: 10
